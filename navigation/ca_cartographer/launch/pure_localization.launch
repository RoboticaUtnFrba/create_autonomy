<launch>
  <param name="/use_sim_time" value="true" />

  <arg name="load_state_filename" default="$(find ca_cartographer)/house_map/house_map.pbstream" />

  <node name="cartographer_node" pkg="cartographer_ros"
      type="cartographer_node" args="
          -configuration_directory $(find ca_cartographer)/config
          -configuration_basename ca_localization_2d.lua
          -load_state_filename $(arg load_state_filename)"
      output="screen">
    <remap from="scan" to="create1/laser/scan" />
    <remap from="imu" to="create1/imu/data" />
    <remap from="odom" to="create1/odom" />
  </node>

  <node name="cartographer_occupancy_grid_node" pkg="cartographer_ros"
      type="cartographer_occupancy_grid_node" args="-resolution 0.05" />

  <!-- <node name="rviz" pkg="rviz" type="rviz" required="true"
      args="-d $(find cartographer_ros)/configuration_files/demo_2d.rviz" /> -->
</launch>
